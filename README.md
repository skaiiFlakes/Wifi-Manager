# WifiManager
## Setup
pip install subprocess

pip install re

pip install concurrent.futures

pip install bs4
## Code Configuration
add necessary path to csv file
## Running The Code
Run in terminal/ cmd
